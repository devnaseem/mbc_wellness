name: mbc_wellness
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  mbc_common:
    path: ../mbc_common
#    git:
#      url: https://github.com/devnaseem/mbc_common.git
#      ref: d4611c7fe3ba1971416246d650b81b280aeaa4db
  mbc_core:
    path: ../mbc_core

  mbc_gallery:
    path: ../mbc_gallery

  cached_network_image: ^3.4.1
  flutter_staggered_grid_view: ^0.7.0
  transparent_image: ^2.0.1
  carousel_slider: ^5.0.0
  photo_view: ^0.15.0
  share_plus: ^10.1.2


#    git:
#      url: https://github.com/devnaseem/mbc_core.git
#      ref: e4cfc80023b2e9b15dc07fa5f96451c0480474f2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  envied_generator: ^0.5.4+1
  retrofit_generator: '>=8.1.2'
  json_serializable: '^6.8.0'
  freezed: ^2.5.7
  riverpod_generator: ^2.4.2
  riverpod_lint: ^2.3.12
  custom_lint_core: 0.6.9 # or an earlier compatible version


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
dependency_overrides:
  meta: 1.15.0


flutter:
  uses-material-design: true

  assets:
    - web/